# SPI Documentation 


# Colette's RUST notes: 
    - no colons for return statements (or return command)
    - name : Type 
    - unwrap() : if there's an error, it panics and gives you information on the error. Use for funtions that return result or option (panicks)
    - ? : Syntax sugar for if internal function returns and error, outer function also returns and error (returns)

# ESP Set-up